# Q02: Greeting

## Description
Write a script that prompts the user for their name, and then prints a customized greeting: `"Hello, [name]! Welcome to Python!"`.

## Example Interaction
```text
Enter your name: Alice
Hello, Alice! Welcome to Python!
```

## Constraints
- Do not hardcode the name in the final output. Use user input.

## Hints
- Use `input()` to get input from the user.
- Use an f-string to format the greeting.

---

source repo :"https://github.com/GreyCoderK/piscine-go/tree/master"

what i want you to do is quite simple but lenghty, 

i want you to go through each of the .go file and get the question that would be askjed for us to provide the file as an answer, just as it would have been in piscine, but the twist is that the question to be asked would bnot be asked in the context of golang, but it would be asked in the context of python, that is , those files in the repo are the answers to a certain piscine golang question, but now i want to achive the exact same thing but using python instead of golang, but i need the correct piuscine questions modded for python, if u need a sample of what the piscine go questions are like please ask and i would provide them else if u can can fine the correct question, let me see them first then you would them modify them into python types that i would use